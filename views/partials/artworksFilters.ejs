<!-- <ul class="nav nav-tabs">

    <li class="active"><a data-toggle="tab" href="">TOUT</a></li>
    <% parsed_setting_desc.filters.forEach(tab => { %>
        <div style="margin-left: 4px;">
            <li><a data-toggle="tab" href="#<%- tab %>"><%- tab.toUpperCase() %></a></li>
        </div>
    <% }) %>
  </ul> -->

  <div class="filter-container">
    <div class="filters button-group js-radio-button-group device-type">
      
        <a href="">
          <button class="button is-checked">All</button>
        </a>
      </button>
      <% parsed_setting_desc.filters.forEach(tab => { %>
        <div style="margin-left: 4px;">
            <button class="button" data-value="<%- tab %>"><%- tab.toUpperCase() %></button>
        </div>
      <% }) %>

    </div>
  </div>

<style>
  .filter-container{
    margin-bottom: 64px;
  }
  .filters {
    margin-top: 40px;
    display: flex;
    display: -webkit-flex;
    flex-wrap: wrap;
    justify-content: center;
  }
  .filters button {
    padding: 10px 25px;
    margin: 0px 5px;
    background-color: #121212;
    border: none;
    border-radius: 10px;
    transition: 150ms;
    color: #ffffff;
    font-size: 16px;
    line-height: 25px;
    font-weight: 700;
    position: relative;
    outline: none;
    cursor: pointer;
  }
  .filters button a{
    text-decoration: none;
    color: #fff;
  }
  .filters button.is-checked {
    background-color: whitesmoke;
    color: #121212;
  }
  .filters button.is-checked a {
    color: inherit;
  }
</style>

<script>
  buttons = document.getElementsByClassName("button");
  for (let i=0; i < buttons.length; i++){
    buttons[i].addEventListener("click", () => {
      for (let k=0; k < buttons.length; k++){
        buttons[k].classList.remove("is-checked");
      }
      buttons[i].classList.add("is-checked");

      // filterFromUrl = window.location.href.split('#')[1];
      filterFromUrl = buttons[i].dataset.value
      console.log(filterFromUrl);
      filterItems()
    })
  }  
</script>