  <div class="filter-container">
    <div class="filters">
        <a href="" class="background-text-color">
          <button class="button is-checked">ALL</button>
        </a>
      </button>
      <% parsed_setting_desc.filters.forEach(tab => { %>
        <div class="background-text-color" style="margin-left: 4px;">
            <button class="button" data-value="<%- tab %>"><%- tab.toUpperCase() %></button>
        </div>
      <% }) %>

    </div>
  </div>

<style>
  .filter-container{
    margin-bottom: 64px;
  }
  .filters {
    margin-top: 40px;
    display: flex;
    display: -webkit-flex;
    flex-wrap: wrap;
    justify-content: start;
  }
  .filters button {
    padding: 10px 25px;
    margin: 5px 5px;
    background-color: transparent;
    border: none;
    transition: 150ms;
    color: var(--custom-filter-button-inactive-color);
    font-size: var(--custom-filter-button-size);
    font-weight: var(--custom-filter-button-weight);
    line-height: 25px;
    position: relative;
    outline: none;
    cursor: pointer;
  }
  .filters button a{
    text-decoration: none;
    color: #fff;
  }
  .filters button.is-checked {
    background-color: Transparent;
    color: var(--custom-filter-button-active-color);
  }
  .filters button.is-checked a {
    color: inherit;
  }

  .button:hover {
    color:var(--custom-filter-button-active-color);
  }
  @media screen and (max-width:980px) {
    .filters button {
      padding: 24px 32px;
      font-size: var(--custom-filter-button-responsive-size);
    }
    
  }
</style>

<%- include('./artworksFiltersScript.ejs'); %>